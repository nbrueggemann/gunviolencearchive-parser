// This file is the entry point to the application.
// To run this app execute "node index.js" at the command line.
var Application = require('./application/app.js');

// Initialize the app
var app = new Application();
app.initialize();  